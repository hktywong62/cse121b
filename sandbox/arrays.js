/*
// example 2
const grades = ["A", "B", "A"];

function convertGradeToPoints(grade) {
  let points = 0;
  if (grade === "A") {
    points = 4;
  } else if (grade === "B") {
    points = 3;
  }
  return points;
}
const gpaPoints = grades.map(convertGradeToPoints);

gpaPoints = [4, 3, 4];

const initialValue = 0;

const sumGpaPoints = gpaPoints.reduce((accmulator, currentValue) => accumulator + currentValue, initialValue);

// Activity 4 = Filter

const fruits = ['watermelon', 'peach', 'apple', 'tomato', 'grape'];

const shortList = fruits.filter( (fruit) => fruit.length > 6);

*/

// Activity 5 - indexOf

Numbers = [12, 34, 21, 54];

let luckNumber = 21;

console.log(luckNumber.indexOf());

indexLucky = luckNumber.indexOf();




